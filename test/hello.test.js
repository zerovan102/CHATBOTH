const assert = require('assert');

describe('Initial Test Setup', () => {
    it('should confirm that tests are working', () => {
        // Tes sederhana yang pasti akan lolos
        expect(true).toBe(true);
    });
});